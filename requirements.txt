# Dependencias del proyecto
# ---------------------------
# El scraping con navegador real usa Selenium (Google Chrome).
# Selenium 4 descarga el chromedriver automáticamente (Selenium Manager),
# así que sólo hace falta tener Google Chrome instalado.
selenium>=4.20

# El resto del sistema (controlador paralelo, almacenamiento) usa solo la
# librería estándar de Python 3.9+ (json, threading, queue, concurrent.futures).
#
# Instalación:
#     python3 -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
